# HTTP Server
server:
  port: 12032  # HTTP (Tomcat) port

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDRS:127.0.0.1:8848}
  application:
    name: ts-order-other-service
  datasource:
    url: jdbc:mysql://${ORDER_OTHER_MYSQL_HOST:10.176.122.1}:${ORDER_OTHER_MYSQL_PORT:3306}/${ORDER_OTHER_MYSQL_DATABASE:ts}?useSSL=false
    username: ${ORDER_OTHER_MYSQL_USER:root}
    password: ${ORDER_OTHER_MYSQL_PASSWORD:Abcd1234#}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

swagger:
  controllerPackage: other.controller